# النماذج المحلية - Local Models

## نظرة عامة

تم إضافة نظام شامل لتشغيل نماذج الذكاء الاصطناعي محلياً على جهاز المستخدم. يدعم هذا النظام:

- ✅ تنزيل نماذج GGUF من Hugging Face (روابط حقيقية ومُختبرة)
- ✅ إدارة النماذج (تنزيل/حذف/اختيار)
- ✅ دعم ممتاز للغة العربية (خاصة نماذج Qwen)
- ✅ نماذج خفيفة للهواتف المحمولة (من 386 ميجا)
- ✅ إعدادات قابلة للتخصيص
- ✅ استدلال في Isolate منفصل (لا يجمد الواجهة)

## النماذج المتاحة (14 نموذج)

### ⭐ نماذج تدعم العربية (موصى بها)

| النموذج | الحجم | الوصف | رابط التنزيل |
|---------|-------|-------|--------------|
| **Qwen 2.5 0.5B Q4** | 491 MB | خفيف جداً، مثالي للهواتف المحدودة | [Hugging Face](https://huggingface.co/Qwen/Qwen2.5-0.5B-Instruct-GGUF) |
| **Qwen 2.5 0.5B Q8** | 676 MB | جودة أعلى، لا يزال خفيفاً | [Hugging Face](https://huggingface.co/Qwen/Qwen2.5-0.5B-Instruct-GGUF) |
| **⭐ Qwen 2.5 1.5B Q4** | 1.05 GB | **موصى به** - توازن ممتاز | [Hugging Face](https://huggingface.co/Qwen/Qwen2.5-1.5B-Instruct-GGUF) |
| **Qwen 2.5 3B Q4** | 2.02 GB | أكثر قدرة، يتطلب 4GB+ RAM | [Hugging Face](https://huggingface.co/Qwen/Qwen2.5-3B-Instruct-GGUF) |
| **Gemma 2 2B Q4** | 1.63 GB | من Google، دعم متعدد اللغات | [Hugging Face](https://huggingface.co/bartowski/gemma-2-2b-it-GGUF) |
| **Phi 3.5 Mini Q4** | 2.32 GB | من Microsoft، استدلال قوي | [Hugging Face](https://huggingface.co/bartowski/Phi-3.5-mini-instruct-GGUF) |
| **Mistral 7B Q2** | 3.08 GB | مضغوط، دعم عربي جيد | [Hugging Face](https://huggingface.co/bartowski/Mistral-7B-Instruct-v0.3-GGUF) |
| **Aya Expanse 8B Q2** | 3.3 GB | من Cohere، دعم عربي ممتاز | [Hugging Face](https://huggingface.co/bartowski/aya-expanse-8b-GGUF) |

### نماذج خفيفة (للأجهزة المحدودة)

| النموذج | الحجم | الوصف |
|---------|-------|-------|
| SmolLM2 360M Q8 | 386 MB | أصغر نموذج، إنجليزي فقط |
| TinyLlama 1.1B Q4 | 638 MB | خفيف، إنجليزي فقط |
| Llama 3.2 1B Q4 | 770 MB | من Meta، سياق 128K |
| SmolLM2 1.7B Q4 | 1.02 GB | فعال، إنجليزي فقط |
| Llama 3.2 3B Q4 | 2.02 GB | من Meta، استدلال جيد |

## كيفية الاستخدام

### 1. الوصول لصفحة النماذج المحلية
- افتح **الإعدادات** ⚙️
- اختر **"النماذج المحلية"** 🧠

### 2. تنزيل نموذج
- اختر النموذج المناسب (⭐ Qwen 2.5 1.5B موصى به للعربية)
- اضغط على زر **"تنزيل"** ⬇️
- انتظر اكتمال التحميل (يظهر شريط التقدم)

### 3. تشغيل النموذج
- بعد التنزيل، اضغط **"تشغيل"** ▶️
- سيتم تحميل النموذج في الذاكرة
- يمكنك الآن استخدامه في المحادثات

### 4. إعدادات النموذج
- اضغط على أيقونة الإعدادات ⚙️ في صفحة النماذج
- عدّل الإعدادات حسب جهازك

## الإعدادات

### إعدادات الأداء
| الإعداد | الوصف | القيمة الافتراضية |
|---------|-------|-------------------|
| طول السياق | عدد الرموز في السياق | 2048 |
| الحد الأقصى للرموز | أقصى عدد رموز في الاستجابة | 512 |
| عدد الخيوط | خيوط المعالج المستخدمة | 4 |
| استخدام GPU | تسريع باستخدام معالج الرسومات | إيقاف |

### إعدادات التوليد
| الإعداد | الوصف | القيمة الافتراضية |
|---------|-------|-------------------|
| درجة الحرارة | التحكم في عشوائية الاستجابة | 0.7 |
| Top P | احتمالية تراكمية للرموز | 0.9 |
| Top K | عدد الرموز المرشحة | 40 |
| عقوبة التكرار | تقليل تكرار الكلمات | 1.1 |

## المتطلبات التقنية

### للهواتف المحمولة
| النموذج | الحد الأدنى RAM | الموصى به |
|---------|-----------------|-----------|
| 0.5B | 2 GB | 3 GB |
| 1.5B | 3 GB | 4 GB |
| 3B | 4 GB | 6 GB |
| 7B+ | 6 GB | 8 GB |

### التخزين المطلوب
- حسب حجم النموذج (386 MB - 3.3 GB)
- مساحة إضافية للتنزيل المؤقت

## البنية التقنية

```
lib/
├── core/
│   ├── providers/
│   │   └── local_model_provider.dart    # مزود النماذج المحلية
│   └── services/
│       └── local_llm/
│           ├── local_model_info.dart    # معلومات النموذج
│           ├── available_models.dart    # قائمة 14 نموذج متاح
│           ├── local_llm_service.dart   # خدمة إدارة النماذج
│           ├── local_inference_service.dart  # خدمة الاستدلال (Isolate)
│           ├── local_chat_service.dart  # خدمة المحادثة
│           ├── local_api_service.dart   # واجهة API
│           └── local_llm.dart           # ملف التصدير
└── features/
    └── local_models/
        ├── pages/
        │   ├── local_models_page.dart   # صفحة النماذج
        │   └── local_model_settings_page.dart  # صفحة الإعدادات
        └── widgets/
            ├── model_card.dart          # بطاقة النموذج
            └── model_filter_chips.dart  # فلاتر البحث
```

## تفعيل الاستدلال الفعلي

### الخطوة 1: إضافة flutter_llama
في ملف `pubspec.yaml`:
```yaml
dependencies:
  flutter_llama: ^1.1.2
```

### الخطوة 2: تشغيل pub get
```bash
flutter pub get
```

### الخطوة 3: إعادة بناء التطبيق
```bash
flutter build apk  # للأندرويد
flutter build ios  # للآيفون
```

### ملاحظة مهمة
- حالياً النظام يعمل في **وضع المحاكاة** (يعرض رسائل تجريبية)
- لتفعيل الاستدلال الحقيقي، يجب إضافة `flutter_llama` وإلغاء التعليق عن الكود في `local_inference_service.dart`

## التكامل مع نظام المحادثة

```dart
import 'package:BuildX/core/services/local_llm/local_llm.dart';

// التحقق من توفر نموذج محلي
final localApi = LocalApiService.instance;
if (localApi.isAvailable) {
  // إرسال رسالة
  final stream = await localApi.sendMessageStream(
    prompt: 'مرحباً، كيف حالك؟',
    messages: conversationHistory,
    systemPrompt: 'أنت مساعد ذكي يتحدث العربية.',
  );
  
  // استقبال الاستجابة
  await for (final chunk in stream) {
    print(chunk); // طباعة كل جزء من الاستجابة
  }
}
```

## الأسئلة الشائعة

### س: أي نموذج أختار للعربية؟
**ج:** Qwen 2.5 1.5B Q4 هو الخيار الأفضل - توازن ممتاز بين الحجم والجودة.

### س: هل يعمل بدون إنترنت؟
**ج:** نعم! بعد تنزيل النموذج، يعمل بالكامل بدون إنترنت.

### س: كم يستهلك من البطارية؟
**ج:** يعتمد على حجم النموذج ومدة الاستخدام. النماذج الصغيرة (0.5B) أقل استهلاكاً.

### س: هل يمكن استخدام أكثر من نموذج؟
**ج:** يمكنك تنزيل عدة نماذج، لكن يمكن تحميل نموذج واحد فقط في الذاكرة.

## الترخيص

هذا المشروع مرخص تحت AGPL-3.0.

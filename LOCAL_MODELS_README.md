# النماذج المحلية - Local Models

## نظرة عامة

تم إضافة نظام شامل لتشغيل نماذج الذكاء الاصطناعي محلياً على جهاز المستخدم. يدعم هذا النظام:

- ✅ تنزيل نماذج GGUF من Hugging Face
- ✅ إدارة النماذج (تنزيل/حذف/اختيار)
- ✅ دعم اللغة العربية
- ✅ نماذج خفيفة للهواتف المحمولة
- ✅ إعدادات قابلة للتخصيص

## النماذج المتاحة

### نماذج تدعم العربية (موصى بها)

| النموذج | الحجم | الوصف |
|---------|-------|-------|
| Qwen 2.5 0.5B | ~400 MB | خفيف جداً، مثالي للهواتف المحدودة |
| Qwen 2.5 1.5B | ~1.1 GB | **موصى به** - توازن ممتاز |
| Qwen 2.5 3B | ~2.1 GB | أكثر قدرة، يتطلب ذاكرة أكبر |
| Gemma 2 2B | ~1.7 GB | من Google، دعم متعدد اللغات |
| Phi 3.5 Mini | ~2.4 GB | من Microsoft، استدلال قوي |
| Mistral 7B Q2 | ~3.1 GB | مضغوط، دعم عربي جيد |

### نماذج خفيفة (للأجهزة المحدودة)

| النموذج | الحجم | الوصف |
|---------|-------|-------|
| SmolLM2 360M | ~386 MB | صغير جداً، قدرات أساسية |
| TinyLlama 1.1B | ~670 MB | خفيف، دعم عربي محدود |
| Llama 3.2 1B | ~770 MB | من Meta، متعدد اللغات |

## كيفية الاستخدام

### 1. الوصول لصفحة النماذج المحلية
- افتح الإعدادات
- اختر "النماذج المحلية"

### 2. تنزيل نموذج
- اختر النموذج المناسب
- اضغط على "تنزيل"
- انتظر اكتمال التحميل

### 3. تشغيل النموذج
- بعد التنزيل، اضغط "تشغيل"
- سيتم تحميل النموذج في الذاكرة
- يمكنك الآن استخدامه في المحادثات

## الإعدادات

### إعدادات الأداء
- **طول السياق**: عدد الرموز في السياق (512-8192)
- **الحد الأقصى للرموز**: أقصى عدد رموز في الاستجابة
- **عدد الخيوط**: خيوط المعالج المستخدمة
- **استخدام GPU**: تسريع باستخدام معالج الرسومات

### إعدادات التوليد
- **درجة الحرارة**: التحكم في عشوائية الاستجابة (0.0-2.0)
- **Top P**: احتمالية تراكمية للرموز
- **Top K**: عدد الرموز المرشحة
- **عقوبة التكرار**: تقليل تكرار الكلمات

### رسالة النظام
يمكنك تخصيص رسالة النظام بالعربية والإنجليزية.

## المتطلبات التقنية

### للهواتف المحمولة
- **الحد الأدنى للذاكرة**: 4 GB RAM
- **الموصى به**: 6+ GB RAM
- **التخزين**: حسب حجم النموذج

### للنماذج الكبيرة (3B+)
- **الذاكرة**: 8+ GB RAM
- **يفضل**: دعم GPU

## البنية التقنية

```
lib/
├── core/
│   ├── providers/
│   │   └── local_model_provider.dart    # مزود النماذج المحلية
│   └── services/
│       └── local_llm/
│           ├── local_model_info.dart    # معلومات النموذج
│           ├── available_models.dart    # قائمة النماذج المتاحة
│           ├── local_llm_service.dart   # خدمة إدارة النماذج
│           ├── local_inference_service.dart  # خدمة الاستدلال
│           ├── local_chat_service.dart  # خدمة المحادثة
│           ├── local_api_service.dart   # واجهة API
│           └── local_llm.dart           # ملف التصدير
└── features/
    └── local_models/
        ├── pages/
        │   ├── local_models_page.dart   # صفحة النماذج
        │   └── local_model_settings_page.dart  # صفحة الإعدادات
        └── widgets/
            ├── model_card.dart          # بطاقة النموذج
            └── model_filter_chips.dart  # فلاتر البحث
```

## ملاحظات للمطورين

### لتفعيل الاستدلال الفعلي
يجب إضافة مكتبة `flutter_llama` أو مكتبة مشابهة لـ llama.cpp:

```yaml
dependencies:
  flutter_llama: ^1.1.2
```

### التكامل مع نظام المحادثة
يمكن استخدام `LocalApiService` للتكامل مع نظام المحادثة الحالي:

```dart
final localApi = LocalApiService.instance;
if (localApi.isAvailable) {
  final stream = await localApi.sendMessageStream(
    prompt: 'مرحباً',
    messages: history,
  );
  await for (final chunk in stream) {
    // معالجة الاستجابة
  }
}
```

## الترخيص

هذا المشروع مرخص تحت AGPL-3.0.
